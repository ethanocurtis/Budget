# ==== Backend ====
API_ENV=development
API_HOST=0.0.0.0
API_PORT=8000
API_LOG_LEVEL=info
CORS_ORIGINS=http://localhost:8080

# JWT
JWT_SECRET=mQjq6BfZJA9EZFyjDHjPlCs5UuFRJtJW87MeZwW4P2vDLn7HYTsmliqA5puSMcKe
JWT_ALG=HS256
JWT_ACCESS_TTL_MIN=15
JWT_REFRESH_TTL_DAYS=7

# Password hashing
ARGON2_TIME_COST=2
ARGON2_MEMORY_COST=51200
ARGON2_PARALLELISM=2

# Optional TOTP 2FA (set to 1 to require)
TOTP_OPTIONAL=1

# Database
DB_DRIVER=postgres
POSTGRES_USER=budgeteer
POSTGRES_PASSWORD=budgeteer
POSTGRES_DB=budgeteer
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Dev fallback (api will auto-use SQLite if DB_DRIVER=sqlite)
SQLITE_PATH=/data/budgeteer.db

# Media storage (mapped to volume)
MEDIA_ROOT=/app/app/media

# Seed
SEED_DEMO=1

# ==== Frontend ====
WEB_PORT=8080
APP_BASE_URL=http://localhost:8080
API_BASE_URL=http://localhost:8000

# ==== Backup ====
BACKUP_DIR=/backups
